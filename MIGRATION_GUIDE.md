# 📋 Руководство по Миграциям

## ⚠️ ВАЖНО: Необходимо создать миграцию!

После добавления новых моделей (Social, Reviews, Streaks, Achievements) нужно обновить базу данных.

### Шаг 1: Создать миграцию

В **Package Manager Console** Visual Studio выполните:

```powershell
Add-Migration AddSocialReviewsStreaksAndAchievements
```

### Шаг 2: Применить миграцию

```powershell
Update-Database
```

### Альтернатива через dotnet CLI:

```powershell
dotnet ef migrations add AddSocialReviewsStreaksAndAchievements
dotnet ef database update
```

---

## 🔄 Если база данных уже существует

Если у вас уже есть база данных и вы хотите начать с чистого листа:

### Вариант 1: Пересоздать базу полностью

```powershell
# В Package Manager Console
Drop-Database
Update-Database
```

### Вариант 2: Через dotnet CLI

```powershell
dotnet ef database drop --force
dotnet ef database update
```

---

## ✅ Проверка после миграции

1. Запустите приложение:
   ```powershell
   dotnet run
   ```

2. В консоли должны появиться сообщения:
   ```
   ✅ Роль 'Admin' создана
   ✅ Роль 'Teacher' создана
   ✅ Роль 'Student' создана
   ✅ Администратор создан: admin@unistart.kz / Admin123!
   ✅ Преподаватель создан: teacher@unistart.kz / Teacher123!
   ✅ Студент создан: student@unistart.kz / Student123!
   ✅ Достижение создано: Первый шаг
   ... (остальные достижения)
   ```

3. Войдите под админом:
   ```bash
   curl -X POST http://localhost:5220/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"email":"admin@unistart.kz","password":"Admin123!"}'
   ```

4. Проверьте JWT токен:
   - Скопируйте полученный `token`
   - Вставьте его на https://jwt.io
   - В payload должен быть:
     ```json
     {
       "http://schemas.microsoft.com/ws/2008/06/identity/claims/role": "Admin",
       ...
     }
     ```

5. Проверьте эндпоинт администратора:
   ```bash
   curl -X GET http://localhost:5220/api/admin/users \
     -H "Authorization: Bearer {ваш_токен}"
   ```

---

## 🐛 Устранение неполадок

### Проблема: Роли не работают

**Решение:** Убедитесь, что вы заново залогинились после обновления базы данных. Старые токены не содержат роли!

### Проблема: Migration pending

**Решение:**
```powershell
Update-Database
```

### Проблема: Entity Framework tools не установлены

**Решение:**
```powershell
dotnet tool install --global dotnet-ef
```

---

## 📊 Новые таблицы в базе данных

После миграции будут созданы следующие таблицы:

- `Tags` - теги для контента
- `Achievements` - достижения
- `UserAchievements` - достижения пользователей
- `QuizReviews` - отзывы на квизы
- `FlashcardSetReviews` - отзывы на наборы карточек
- `UserStreaks` - серии дней обучения
- `UserFollows` - подписки между пользователями
- `ActivityFeeds` - лента активности

---

## 🎯 Тестирование ролей

После успешной миграции проверьте все три роли:

### 1. Админ (admin@unistart.kz / Admin123!)
- Должен иметь доступ к `/api/admin/*`
- GET `/api/admin/users` - работает ✅
- GET `/api/admin/stats` - работает ✅

### 2. Преподаватель (teacher@unistart.kz / Teacher123!)
- Должен иметь доступ к `/api/teacher/*`
- POST `/api/teacher/quizzes/public` - работает ✅
- GET `/api/admin/users` - НЕ работает ❌ (403 Forbidden)

### 3. Студент (student@unistart.kz / Student123!)
- Должен иметь доступ к `/api/student/*`
- GET `/api/student/my-stats` - работает ✅
- GET `/api/admin/users` - НЕ работает ❌ (403 Forbidden)
- GET `/api/teacher/quizzes/my` - НЕ работает ❌ (403 Forbidden)

---

Удачи! 🚀
