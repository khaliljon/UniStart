# Объяснение: Что означает "Освоено" (MasteredCards)?

## Дата: 12 декабря 2025

## ❓ Вопросы пользователя

1. **"Не считает сколько карточек освоено. Я так понял 'освоено' это те в которых студент правильно ответил же?"**
2. **"Что означает под зеленой иконкой значок блокнота с числами? Типа сколько карточек просмотрел или что?"**

## ✅ Ответы

### 1. Что означает "Освоено" (IsMastered)?

**НЕТ, "освоено" ≠ "правильно ответил один раз"!**

Карточка считается **освоенной (IsMastered)** только при выполнении **двух условий**:

```csharp
progress.IsMastered = progress.Repetitions >= 3 && progress.EaseFactor >= 2.5;
```

**Условия освоения:**
1. ✅ **Минимум 3 успешных повторения подряд** (с оценкой >= 3)
2. ✅ **Коэффициент легкости >= 2.5** (EaseFactor)

### Шкала оценок (quality) при review карточки:

| Оценка | Описание |
|--------|----------|
| **5** | Идеальный ответ |
| **4** | Правильный ответ после колебаний |
| **3** | Правильный ответ с трудом |
| **2** | Неправильный ответ, но вспомнилось при показе |
| **1** | Неправильный ответ, показалось знакомым |
| **0** | Полное незнание |

**Важно:** Если оценка < 3, прогресс **сбрасывается** (Repetitions = 0), и нужно начинать заново!

### Алгоритм Spaced Repetition (SM-2)

Система использует алгоритм **SuperMemo 2** для интервального повторения:

1. **1-е повторение** (quality >= 3): следующее через **1 день**
2. **2-е повторение** (quality >= 3): следующее через **6 дней**
3. **3-е повторение** (quality >= 3): интервал умножается на EaseFactor
4. При **Repetitions >= 3 и EaseFactor >= 2.5** → карточка **освоена**!

### Пример прогресса:

```
День 1: Review с quality=4 → Repetitions=1, NextReview через 1 день
День 2: Review с quality=5 → Repetitions=2, NextReview через 6 дней
День 8: Review с quality=4 → Repetitions=3, IsMastered=true (если EaseFactor >= 2.5)
```

### 2. Терминология

| Термин | Описание | Условие |
|--------|----------|---------|
| **ReviewedCards** (Просмотрено) | Карточки, которые открыл хотя бы раз | `LastReviewedAt != null` |
| **MasteredCards** (Освоено) | Карточки, которые правильно ответил минимум 3 раза подряд | `Repetitions >= 3 && EaseFactor >= 2.5` |
| **CompletedFlashcardSets** (Завершено наборов) | Наборы, в которых все карточки освоены | `IsCompleted = true` |

### 3. Значки в интерфейсе

На вашем скриншоте:

```
✅ 0  ← Зеленая галочка: Освоенные карточки (MasteredCards)
📋 2  ← Блокнот с числом: Просмотренные карточки (ReviewedCards)
```

**Это правильно!** Вы просмотрели 2 карточки, но ни одна не освоена (нужно минимум 3 успешных повторения).

## 🐛 Исправленные проблемы

### Проблема 1: Неправильные имена полей в API

**Было:**
```csharp
FlashcardProgress = new
{
    TotalCardsMastered = masteredCardsCount,  // Неправильно!
    SetDetails = flashcardSetDetails
}
```

**Стало:**
```csharp
FlashcardProgress = new
{
    MasteredCards = masteredCardsCount,  // Правильно!
    SetDetails = flashcardSetDetails
}
```

### Проблема 2: Неправильный подсчет прогресса по наборам

**Было:** Использовались данные из `UserFlashcardSetAccess.CardsStudiedCount` (устаревшее поле)

**Стало:** Данные берутся из `UserFlashcardProgress`:
```csharp
var flashcardProgressBySet = await _context.UserFlashcardProgresses
    .Where(p => p.UserId == studentId)
    .Include(p => p.Flashcard)
    .GroupBy(p => p.Flashcard.FlashcardSetId)
    .Select(g => new
    {
        SetId = g.Key,
        ReviewedCards = g.Count(p => p.LastReviewedAt != null),  // Просмотрено
        MasteredCards = g.Count(p => p.IsMastered)  // Освоено
    })
    .ToDictionaryAsync(x => x.SetId);
```

### Проблема 3: Отсутствие поля `LastAccessed`

**Было:** Поля `CompletedAt`, `FirstAccessedAt`, `LastAccessedAt`, `AccessCount` (избыточно)

**Стало:** Одно поле `LastAccessed` для упрощения:
```csharp
LastAccessed = a.LastAccessedAt ?? a.FirstAccessedAt
```

## 📊 Обновленная структура данных

### FlashcardSetProgressDetail (для админа/учителя):

```typescript
interface FlashcardSetProgressDetail {
  setId: number;
  setTitle: string;
  totalCards: number;
  reviewedCards: number;  // Просмотрено хотя бы раз
  masteredCards: number;  // Полностью освоено (3+ повторения)
  isCompleted: boolean;   // Все карточки освоены
  lastAccessed: string;   // Последний доступ к набору
}
```

### FlashcardProgress (общая статистика):

```typescript
interface FlashcardProgress {
  setsAccessed: number;        // Наборов открыто
  setsCompleted: number;       // Наборов завершено
  totalCardsReviewed: number;  // Карточек просмотрено
  masteredCards: number;       // Карточек освоено
  setDetails: FlashcardSetProgressDetail[];  // Детали по каждому набору
}
```

## 🎯 Рекомендации для пользователя

### Как освоить карточку:

1. **Откройте карточку** и ответьте на нее
2. **Оцените свой ответ** (0-5):
   - 5 = идеально знаю
   - 4 = знаю хорошо
   - 3 = знаю с трудом
   - 2 или ниже = не знаю (прогресс сбросится!)
3. **Повторите минимум 3 раза** с оценкой >= 3
4. **Дождитесь, пока EaseFactor >= 2.5** (автоматически растет при хороших оценках)
5. После 3-го успешного повторения карточка станет **освоенной**!

### Почему показывает 0 освоенных?

- Вы просмотрели 2 карточки, но **не повторили их 3 раза**
- Или оценки были < 3, и прогресс сбросился
- Или EaseFactor еще < 2.5

### Как завершить набор?

- Освойте **все карточки** в наборе (каждую минимум 3 раза с оценкой >= 3)
- Тогда `IsCompleted = true` и набор будет считаться завершенным

## 📝 Измененные файлы

### Backend:
- `Controllers\AdminController.cs` (строки 1049-1085, 1137-1144)
- `Controllers\TeacherController.cs` (строки 390-420, 478-486)

### Frontend:
- `unistart-frontend\src\types\index.ts` (FlashcardSetProgressDetail)
- `unistart-frontend\src\pages\StudentDetailPage.tsx` (отображение)

## ✅ Статус

**Исправлено:** Все поля синхронизированы, данные берутся из правильных источников (`UserFlashcardProgress`), терминология уточнена.

**Протестировано:** Проект успешно компилируется.

**Следующий шаг:** Перезапустить бэкенд и фронтенд, проверить отображение в UI.


