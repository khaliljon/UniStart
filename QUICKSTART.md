# 🚀 UniStart - Быстрый старт

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

✅ Убедитесь, что у вас установлено:
- .NET 8.0 SDK
- PostgreSQL 14+
- Visual Studio 2022 / VS Code / Rider
- Git

---

## ⚡ БЫСТРЫЙ ЗАПУСК (5 минут)

### 1️⃣ Установите EF Core CLI Tools

```powershell
dotnet tool install --global dotnet-ef
```

### 2️⃣ Настройте строку подключения к БД

Откройте `appsettings.json` и проверьте настройки PostgreSQL:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=UniStart;Username=postgres;Password=YOUR_PASSWORD;Port=5432"
  }
}
```

**ВАЖНО:** Замените `YOUR_PASSWORD` на ваш реальный пароль PostgreSQL!

### 3️⃣ Примените миграции

```powershell
dotnet ef database update
```

Это создаст базу данных и все таблицы автоматически.

### 4️⃣ Запустите проект

```powershell
dotnet watch run
```

### 5️⃣ Откройте Swagger

После запуска откройте в браузере:
```
https://localhost:7xxx/swagger
```

(Точный порт будет показан в консоли после запуска)

---

## 🧪 ТЕСТИРОВАНИЕ API

### Регистрация пользователя

При первом запуске база автоматически заполняется тестовыми данными, включая пользователя:

**Email:** `test@unistart.kz`  
**Password:** `Test123!`

### Или создайте нового через API:

**POST** `/api/auth/register`
```json
{
  "email": "yourname@example.com",
  "password": "YourPassword123!",
  "firstName": "Ваше имя",
  "lastName": "Ваша фамилия"
}
```

### Вход в систему

**POST** `/api/auth/login`
```json
{
  "email": "test@unistart.kz",
  "password": "Test123!"
}
```

Вы получите JWT токен в ответе:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "email": "test@unistart.kz",
  "firstName": "Айдар",
  "lastName": "Тестов",
  "expiresAt": "2025-11-13T..."
}
```

### Использование токена

В Swagger:
1. Нажмите кнопку **Authorize** (замочек) вверху справа
2. Введите: `Bearer ВАШ_ТОКЕН`
3. Нажмите **Authorize**

Теперь можно вызывать защищенные endpoints!

---

## 📚 ДОСТУПНЫЕ ENDPOINTS

### 🔐 Authentication (`/api/auth`)

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/profile` - Получить профиль (требует токен)
- `PUT /api/auth/profile` - Обновить профиль (требует токен)

### 📇 Flashcards (`/api/flashcards`)

**Наборы карточек:**
- `GET /api/flashcards/sets` - Все наборы
- `GET /api/flashcards/sets/{id}` - Набор по ID
- `POST /api/flashcards/sets` - Создать набор
- `PUT /api/flashcards/sets/{id}` - Обновить набор
- `DELETE /api/flashcards/sets/{id}` - Удалить набор

**Карточки:**
- `GET /api/flashcards/sets/{setId}/review` - Карточки для повторения
- `GET /api/flashcards/cards/{id}` - Карточка по ID
- `POST /api/flashcards/cards` - Создать карточку
- `PUT /api/flashcards/cards/{id}` - Обновить карточку
- `DELETE /api/flashcards/cards/{id}` - Удалить карточку
- `POST /api/flashcards/cards/review` - Отметить повторение (Spaced Repetition)

### 📝 Quizzes (`/api/quizzes`)

**Тесты:**
- `GET /api/quizzes` - Все тесты (фильтры: subject, difficulty)
- `GET /api/quizzes/{id}` - Тест по ID (для прохождения)
- `POST /api/quizzes` - Создать тест
- `PUT /api/quizzes/{id}` - Обновить тест
- `DELETE /api/quizzes/{id}` - Удалить тест
- `PATCH /api/quizzes/{id}/publish` - Опубликовать/снять с публикации

**Прохождение:**
- `POST /api/quizzes/submit` - Отправить ответы и получить результат
- `GET /api/quizzes/{quizId}/attempts` - История попыток
- `GET /api/quizzes/{quizId}/statistics` - Статистика по тесту

---

## 🎯 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Изучение карточек

```bash
# 1. Получить карточки для повторения
GET /api/flashcards/sets/1/review

# 2. Получить конкретную карточку
GET /api/flashcards/cards/1

# 3. Отметить повторение (quality: 0-5)
POST /api/flashcards/cards/review
{
  "flashcardId": 1,
  "quality": 4
}
```

**Quality шкала:**
- `5` - Идеальный ответ
- `4` - Правильно после колебаний
- `3` - Правильно с трудом
- `2` - Неправильно, но вспомнил при подсказке
- `1` - Неправильно, но показалось знакомым
- `0` - Полное незнание

### Пример 2: Прохождение теста

```bash
# 1. Получить список тестов
GET /api/quizzes?subject=Математика&difficulty=Medium

# 2. Получить тест для прохождения
GET /api/quizzes/1

# 3. Отправить ответы
POST /api/quizzes/submit
{
  "quizId": 1,
  "timeSpentSeconds": 450,
  "userAnswers": {
    "1": [1],      // Вопрос 1 -> Ответ 1
    "2": [2],      // Вопрос 2 -> Ответ 2
    "3": [2, 3]    // Вопрос 3 -> Ответы 2 и 3 (множественный выбор)
  }
}
```

---

## 🗄️ ТЕСТОВЫЕ ДАННЫЕ

После первого запуска база автоматически заполняется:

**Пользователь:**
- Email: `test@unistart.kz`
- Password: `Test123!`

**Наборы карточек (4 набора):**
1. Алгебра - Основы (5 карточек)
2. Физика - Механика (4 карточки)
3. История Казахстана - Ключевые даты (3 карточки)
4. English - Irregular Verbs (4 карточки)

**Тесты (3 теста):**
1. Квадратные уравнения (Математика, Medium, 3 вопроса)
2. Основы механики (Физика, Easy, 2 вопроса)
3. История Казахстана - Важные даты (История, Easy, 2 вопроса)

---

## 🔧 КОМАНДЫ ДЛЯ РАЗРАБОТКИ

### Работа с миграциями

```powershell
# Создать новую миграцию
dotnet ef migrations add МоёИмяМиграции

# Применить миграции
dotnet ef database update

# Откатить последнюю миграцию
dotnet ef migrations remove

# Откатить все миграции (очистить БД)
dotnet ef database update 0

# Список всех миграций
dotnet ef migrations list

# Создать SQL скрипт
dotnet ef migrations script
```

### Сборка и запуск

```powershell
# Сборка проекта
dotnet build

# Запуск (обычный)
dotnet run

# Запуск с hot reload (авто-перезагрузка)
dotnet watch run

# Очистка
dotnet clean

# Восстановление зависимостей
dotnet restore
```

### Docker (если используете)

```powershell
# Запустить PostgreSQL
docker-compose up -d

# Остановить
docker-compose down

# Логи
docker-compose logs -f postgres
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Cannot connect to database"

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте строку подключения в `appsettings.json`
3. Проверьте пароль и порт (обычно 5432, но может быть 5151)

```powershell
# Проверка статуса PostgreSQL (Windows)
Get-Service postgresql*

# Запуск PostgreSQL
Start-Service postgresql-x64-14
```

### Проблема: "dotnet ef not found"

**Решение:**
```powershell
dotnet tool install --global dotnet-ef
dotnet tool update --global dotnet-ef
```

### Проблема: "Migration already applied"

**Решение:**
```powershell
# Откатить все миграции
dotnet ef database update 0

# Применить заново
dotnet ef database update
```

### Проблема: "JWT token invalid"

**Решение:**
1. Проверьте, что токен не истек (срок действия 7 дней)
2. Войдите заново для получения нового токена
3. Убедитесь, что используете формат: `Bearer ВАШ_ТОКЕН`

---

## 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **ROADMAP.md** - Полная дорожная карта проекта
- **ARCHITECTURE.md** - Архитектурные решения и best practices
- **Swagger** - Интерактивная документация API (https://localhost:7xxx/swagger)

---

## 🎓 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Ознакомьтесь со всеми API через Swagger
2. ✅ Протестируйте Spaced Repetition на карточках
3. ✅ Пройдите тестовые тесты
4. ✅ Прочитайте ARCHITECTURE.md для понимания структуры
5. ✅ Начните разработку фронтенда (React)

---

**Нужна помощь?** Проверьте логи в консоли или создайте Issue!

**Удачи в разработке UniStart! 🚀🎓**
