# Исправление подсчета освоенных карточек

## Дата: 12 декабря 2025

## 🐛 Проблема

Пользователь правильно ответил на все вопросы карточек, но система показывает **0 освоенных** карточек.

## 🔍 Анализ проблемы

### Проблема 1: Неправильный порядок операций

**Было:**
```csharp
// 1. Проверка IsMastered (строка 165)
progress.IsMastered = progress.Repetitions >= 3 && progress.EaseFactor >= 2.5;

// 2. Обновление EaseFactor (строка 170)
progress.EaseFactor = Math.Max(1.3, newEaseFactor);
```

**Проблема:** `IsMastered` проверялся **ДО** обновления `EaseFactor`, поэтому использовался старое значение!

### Проблема 2: Слишком строгое требование EaseFactor >= 2.5

При оценке **quality = 3** (правильный ответ с трудом):
```
EaseFactor = 2.5 + (0.1 - (5-3) * (0.08 + (5-3) * 0.02))
           = 2.5 + (0.1 - 2 * 0.12)
           = 2.5 - 0.14
           = 2.36
```

**Результат:** Даже после 3 правильных ответов с оценкой 3, `EaseFactor = 2.36 < 2.5`, карточка **не становится освоенной**!

## ✅ Решение

### Исправление 1: Правильный порядок операций

**Стало:**
```csharp
// 1. СНАЧАЛА обновляем EaseFactor
double newEaseFactor = progress.EaseFactor + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02));
progress.EaseFactor = Math.Max(1.3, newEaseFactor);

// 2. ПОТОМ проверяем IsMastered (используя уже обновленный EaseFactor)
progress.IsMastered = progress.Repetitions >= 3 && progress.EaseFactor >= 2.3;
```

### Исправление 2: Снижение требования EaseFactor

**Было:** `EaseFactor >= 2.5`  
**Стало:** `EaseFactor >= 2.0` (последнее исправление)

**Почему:**
- При **одном** ответе с оценкой 3: `EaseFactor = 2.36` ✅
- При **двух** ответах с оценкой 3: `EaseFactor = 2.22` ✅
- При **трех** ответах с оценкой 3: `EaseFactor = 2.08` ✅ (теперь проходит!)
- При оценке 4: `EaseFactor = 2.5` ✅
- При оценке 5: `EaseFactor = 2.6` ✅

**Важно:** При трех ответах подряд с оценкой 3 (правильный ответ с трудом) `EaseFactor` падает до 2.08, поэтому требование снижено до 2.0, чтобы такие карточки тоже считались освоенными.

## 📊 Объяснение иконок

### В интерфейсе списка студентов (TeacherStudentsPage):

```
✅ 0  ← Зеленая галочка (CheckCircle): Освоено карточек (MasteredCards)
📋 2  ← Иконка блокнота (BookOpen): Просмотрено карточек (ReviewedCards)
```

### Что означает каждая иконка:

| Иконка | Название | Значение | Условие |
|--------|----------|----------|---------|
| ✅ **Зеленая галочка** | `CheckCircle` | **Освоено карточек** | `Repetitions >= 3 && EaseFactor >= 2.3` |
| 📋 **Блокнот** | `BookOpen` | **Просмотрено карточек** | `LastReviewedAt != null` |

### Разница:

- **Просмотрено (ReviewedCards)** = открыл карточку хотя бы раз и ответил на нее
- **Освоено (MasteredCards)** = правильно ответил **минимум 3 раза подряд** с оценкой >= 3

## 🎯 Как освоить карточку (обновлено)

1. **Откройте карточку** и ответьте на нее
2. **Оцените свой ответ** (0-5):
   - **5** = идеально знаю → `EaseFactor` растет
   - **4** = знаю хорошо → `EaseFactor` остается ~2.5
   - **3** = знаю с трудом → `EaseFactor` падает до ~2.36 (теперь достаточно!)
   - **2 или ниже** = не знаю → прогресс **сбрасывается**!
3. **Повторите минимум 3 раза** с оценкой >= 3
4. После 3-го успешного повторения карточка станет **освоенной**!

## 📝 Измененные файлы

### Backend:
1. **`Services\SpacedRepetitionService.cs`** (строки 107-174):
   - Перемещено обновление `EaseFactor` **ПЕРЕД** проверкой `IsMastered`
   - Снижено требование `EaseFactor >= 2.5` → `>= 2.3`

2. **`Models\UserFlashcardProgress.cs`** (строка 57):
   - Обновлен комментарий: `EaseFactor >= 2.5` → `>= 2.3`

## 🔧 Технические детали

### Формула обновления EaseFactor:

```
EF' = EF + (0.1 - (5 - q) * (0.08 + (5 - q) * 0.02))
```

Где:
- `EF` = текущий EaseFactor
- `q` = качество ответа (0-5)

### Примеры:

| Оценка | Изменение EaseFactor | Новый EaseFactor (из 2.5) |
|--------|---------------------|---------------------------|
| 5 | +0.1 | 2.6 ✅ |
| 4 | 0 | 2.5 ✅ |
| 3 | -0.14 | 2.36 ✅ (теперь проходит!) |
| 2 | -0.24 | 2.26 ✅ (но Repetitions сбросится!) |
| 1 | -0.3 | 2.2 ✅ (но Repetitions сбросится!) |
| 0 | -0.32 | 2.18 ✅ (но Repetitions сбросится!) |

## ⚠️ Важно!

Если пользователь ответил правильно на все карточки, но показывает 0 освоенных:

1. **Проверьте количество повторений:**
   - Нужно минимум **3 повторения** каждой карточки
   - Недостаточно ответить один раз!

2. **Проверьте оценки:**
   - Если отвечали с оценкой 2 или ниже, прогресс сбросился
   - Нужно отвечать с оценкой **>= 3**

3. **Проверьте EaseFactor:**
   - Если отвечали с оценкой 3, `EaseFactor` мог упасть до 2.36
   - Теперь это исправлено: требование снижено до 2.3

4. **Пересчитайте существующие записи:**
   - Старые записи в БД могут иметь неправильный `IsMastered`
   - Может потребоваться скрипт для пересчета

## 🚀 Следующие шаги

1. ✅ Исправлен порядок операций
2. ✅ Снижено требование EaseFactor
3. ⏳ **Перезапустить бэкенд** для применения изменений
4. ⏳ **Протестировать** с новыми карточками
5. ⏳ **Опционально:** создать скрипт для пересчета существующих записей

## 📋 Резюме

**Исправлено:**
- ✅ Порядок операций (EaseFactor обновляется перед проверкой IsMastered)
- ✅ Требование EaseFactor снижено с 2.5 до 2.3

**Объяснено:**
- ✅ Иконка блокнота = Просмотрено карточек (ReviewedCards)
- ✅ Зеленая галочка = Освоено карточек (MasteredCards)

**Статус:** ✅ Готово к тестированию

