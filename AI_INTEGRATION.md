# Интеграция искусственного интеллекта в UniStart

## Обзор AI функционала

UniStart использует несколько уровней AI технологий для персонализации обучения абитуриентов:

## 1. Machine Learning (ML.NET)

### Адаптивное обучение с интервальными повторениями
- **Технология**: ML.NET FastTree Regression
- **Цель**: Предсказание оптимального времени повторения flashcards
- **Модель**: `Services/AI/MLPredictionService.cs`

#### Возможности:
- ✅ Анализ паттернов запоминания каждого пользователя
- ✅ Предсказание оптимального времени следующего повторения
- ✅ Адаптация к скорости забывания (forgetting curve)
- ✅ Персональная генерация учебного плана

#### API Endpoints:
```
GET  /api/ai/adaptive-learning/next-review/{flashcardId} - Предсказание времени повторения
GET  /api/ai/adaptive-learning/study-plan               - Персональный план обучения
POST /api/ai/adaptive-learning/retrain                  - Переобучение модели (Admin)
GET  /api/ai/adaptive-learning/model-status             - Статус ML модели
```

#### Используемые признаки (Features):
- EaseFactor - коэффициент легкости карточки (1.3-5.0)
- Interval - текущий интервал повторения (дни)
- Repetitions - количество повторений
- DaysSinceLastReview - дней с последнего повторения
- UserRetentionRate - средний retention rate пользователя
- UserForgettingSpeed - скорость забывания
- CorrectAfterBreak - успешные ответы после перерывов

#### Процесс обучения:
1. Сбор данных из `UserFlashcardProgresses` (последние 90 дней)
2. Анализ `UserLearningPatterns` для каждого пользователя
3. Обучение FastTree регрессии на исторических данных
4. Сохранение модели в `Models/flashcard_review_model.zip`
5. Использование для предсказаний в реальном времени

## 2. Рекомендательная система университетов

### Content-Based Filtering
- **Технология**: Алгоритм на основе контента
- **Цель**: Подбор университетов по профилю абитуриента
- **Модель**: `Services/AI/UniversityRecommendationService.cs`

#### Возможности:
- ✅ Анализ успеваемости по предметам из квизов и экзаменов
- ✅ Сопоставление с требованиями университетов
- ✅ Расчет вероятности поступления
- ✅ Учет бюджета и географических предпочтений
- ✅ Кэширование рекомендаций (7 дней)

#### API Endpoints:
```
GET  /api/ai/recommendations/universities  - Топ-N рекомендаций университетов
GET  /api/ai/recommendations/profile       - Профиль пользователя
PUT  /api/ai/recommendations/preferences   - Обновить предпочтения
POST /api/ai/recommendations/{id}/viewed   - Отметить просмотр
POST /api/ai/recommendations/{id}/rate     - Оценить рекомендацию (1-5)
```

#### Алгоритм расчета Match Score (0-100):
1. **Совпадение по предметам (40%)** - средний балл по профильным предметам
2. **Карьерная цель (20%)** - соответствие программам университета
3. **Бюджет (15%)** - соответствие стоимости обучения
4. **Локация (10%)** - предпочтительный город
5. **Проходной балл (15%)** - сравнение с минимальными требованиями

#### Построение профиля пользователя:
```csharp
UserProfile {
    SubjectScores: Dictionary<string, double>  // Оценки по предметам
    TotalTestsTaken: int                       // Всего тестов
    TotalExamsTaken: int                       // Всего экзаменов
    AverageExamScore: double                   // Средний балл
    Strengths: List<string>                    // Топ-3 сильных предмета
    Weaknesses: List<string>                   // Топ-3 слабых предмета
    LearningProgress: double                   // % освоенных карточек
    PreferredCity: string?                     // Желаемый город
    MaxBudget: decimal?                        // Максимальный бюджет
    CareerGoal: string?                        // Карьерная цель
}
```

## 3. Интеллектуальные рекомендации контента

### Персонализация обучения
- **Технология**: Анализ паттернов + эвристики
- **Цель**: Рекомендовать релевантный контент
- **Модель**: `Services/AI/ContentRecommendationService.cs`

#### Возможности:
- ✅ Рекомендации квизов по слабым темам
- ✅ Подбор экзаменов под карьерные цели
- ✅ Рекомендации flashcard наборов
- ✅ Определение следующей темы для изучения
- ✅ Персонализированные советы по улучшению

#### API Endpoints:
```
GET /api/ai/content/quizzes/recommended      - Рекомендованные квизы
GET /api/ai/content/exams/recommended        - Рекомендованные экзамены
GET /api/ai/content/flashcards/recommended   - Рекомендованные наборы
GET /api/ai/content/next-topic               - Следующая тема
GET /api/ai/content/tips                     - Персональные советы
GET /api/ai/content/dashboard                - Панель рекомендаций
```

#### Типы персонализированных советов:
- 💡 Анализ успеваемости (средний балл)
- ⚠️ Выявление слабых сторон
- 🎯 Рекомендации по прогрессу
- 🔄 Советы по частоте повторений
- 🧠 Оптимизация запоминания
- ⏰ Мотивационные напоминания

## 4. AI Генерация контента (Готово к интеграции)

### Генеративный AI (OpenAI/Azure OpenAI)
- **Технология**: GPT-4 или аналоги
- **Цель**: Автоматическая генерация вопросов и объяснений
- **Модель**: `Services/AI/AIContentGeneratorService.cs`

#### Возможности (после настройки API):
- 🤖 Генерация вопросов по любому предмету
- 📝 Автоматические объяснения ответов
- 💬 Интеллектуальные подсказки
- 📚 Резюме учебных материалов

#### API Endpoints:
```
GET  /api/ai/generate/status       - Статус AI генератора
POST /api/ai/generate/questions    - Сгенерировать вопросы
POST /api/ai/generate/explanation  - Получить объяснение
POST /api/ai/generate/hint         - Получить подсказку
POST /api/ai/generate/summary      - Создать резюме
```

#### Настройка (appsettings.json):
```json
{
  "AI": {
    "OpenAI": {
      "ApiKey": "YOUR_OPENAI_API_KEY",
      "Endpoint": "https://api.openai.com/v1/chat/completions",
      "Model": "gpt-4",
      "MaxTokens": 1000,
      "Temperature": 0.7
    }
  }
}
```

## Архитектура AI системы

```
┌─────────────────────────────────────────────────────────┐
│                    UniStart API                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────┐  ┌──────────────────────────────┐  │
│  │   ML.NET       │  │   Recommendation System      │  │
│  │                │  │                              │  │
│  │ • FastTree     │  │ • Content-Based Filtering   │  │
│  │ • Regression   │  │ • User Profiling            │  │
│  │ • Predictions  │  │ • Match Scoring             │  │
│  └────────────────┘  └──────────────────────────────┘  │
│                                                          │
│  ┌────────────────┐  ┌──────────────────────────────┐  │
│  │  Content AI    │  │   OpenAI Integration         │  │
│  │                │  │                              │  │
│  │ • Smart Tips   │  │ • Content Generation        │  │
│  │ • Next Topics  │  │ • Explanations              │  │
│  │ • Weak Points  │  │ • Question Creation         │  │
│  └────────────────┘  └──────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  PostgreSQL  │
                    │              │
                    │ • User Data  │
                    │ • Patterns   │
                    │ • History    │
                    └──────────────┘
```

## Модели данных для AI

### UserLearningPattern
```csharp
{
    UserId: string
    AverageRetentionRate: double       // Средний % запоминания (0-100)
    ForgettingSpeed: double            // Скорость забывания (0.1-5.0)
    OptimalSessionLength: int          // Оптимальная длина сессии (мин)
    PreferredStudyTime: TimeSpan?      // Предпочтительное время
    SessionsProcessed: int             // Обработано сессий
    LastUpdated: DateTime
}
```

### UniversityRecommendation
```csharp
{
    UserId: string
    UniversityId: int
    MatchScore: double                 // Оценка соответствия (0-100)
    AdmissionProbability: double       // Вероятность поступления (0-100)
    ReasonsJson: string                // JSON массив причин
    Rank: int                          // Позиция в рекомендациях
    IsViewed: bool
    UserRating: int?                   // Оценка пользователя (1-5)
    CreatedAt: DateTime
}
```

### FlashcardReviewPrediction
```csharp
{
    FlashcardId: int
    UserId: string
    OptimalReviewHours: int            // Оптимальное время (часы)
    Confidence: float                  // Уверенность модели (0-1)
    RecommendedReviewDate: DateTime    // Рекомендуемая дата
    Reason: string                     // Объяснение
    CreatedAt: DateTime
}
```

## Процесс работы ML модели

### 1. Сбор данных
```csharp
// Берем историю за 90 дней
var progresses = await _context.UserFlashcardProgresses
    .Where(p => p.LastReviewedAt >= cutoffDate && p.Repetitions > 0)
    .Include(p => p.User)
    .ToListAsync();
```

### 2. Подготовка признаков
```csharp
var trainingData = new FlashcardReviewData {
    EaseFactor = progress.EaseFactor,
    Interval = progress.Interval,
    Repetitions = progress.Repetitions,
    DaysSinceLastReview = daysSince,
    UserRetentionRate = pattern.AverageRetentionRate,
    UserForgettingSpeed = pattern.ForgettingSpeed,
    CorrectAfterBreak = correctCount,
    OptimalReviewHours = targetHours  // Label
};
```

### 3. Обучение модели
```csharp
var pipeline = _mlContext.Transforms
    .Concatenate("Features", featureColumns)
    .Append(_mlContext.Transforms.NormalizeMinMax("Features"))
    .Append(_mlContext.Regression.Trainers.FastTree(
        labelColumnName: "OptimalReviewHours",
        numberOfLeaves: 20,
        numberOfTrees: 100,
        minimumExampleCountPerLeaf: 10
    ));

var model = pipeline.Fit(trainingData);
```

### 4. Предсказание
```csharp
var predictionEngine = _mlContext.Model
    .CreatePredictionEngine<FlashcardReviewData, FlashcardReviewPredictionResult>(_model);

var prediction = predictionEngine.Predict(inputData);
var hours = Math.Max(1, Math.Min(8760, (int)prediction.Score));
```

## Метрики и оценка

### ML.NET модель
- **Метрика**: R² (коэффициент детерминации)
- **Минимум данных**: 100 примеров для обучения
- **Переобучение**: Еженедельно (можно настроить)
- **Fallback**: SM-2 алгоритм при отсутствии модели

### Рекомендации университетов
- **Обновление**: Каждые 7 дней или по запросу (forceRefresh)
- **Кэширование**: В базе данных
- **Обратная связь**: Рейтинг 1-5 от пользователей

## Планы развития AI

### Краткосрочные (1-3 месяца):
- [ ] Интеграция с OpenAI API для генерации контента
- [ ] A/B тестирование рекомендаций
- [ ] Расширенная аналитика обучения
- [ ] Collaborative filtering для рекомендаций

### Среднесрочные (3-6 месяцев):
- [ ] Deep Learning модели для предсказаний
- [ ] Распознавание образов в математических задачах
- [ ] Голосовой ассистент для обучения
- [ ] Автоматическая оценка развернутых ответов

### Долгосрочные (6+ месяцев):
- [ ] Персональный AI тьютор для каждого студента
- [ ] Предсказание успеваемости на экзаменах
- [ ] Адаптивная генерация учебных планов
- [ ] Интеграция с внешними образовательными платформами

## Использование API

### Пример: Получение рекомендаций университетов
```http
GET /api/ai/recommendations/universities?topN=10&forceRefresh=false
Authorization: Bearer YOUR_JWT_TOKEN

Response:
{
    "total": 10,
    "recommendations": [
        {
            "id": 123,
            "university": {
                "id": 45,
                "name": "КазНУ им. аль-Фараби",
                "city": "Алматы",
                "country": "Казахстан"
            },
            "matchScore": 87.5,
            "admissionProbability": 75.0,
            "reasons": [
                "Отличные результаты по профильным предметам (Математика, Физика)",
                "Университет находится в желаемом городе (Алматы)",
                "Ваши результаты выше минимального порога"
            ],
            "rank": 1
        }
    ]
}
```

### Пример: Получение учебного плана
```http
GET /api/ai/adaptive-learning/study-plan
Authorization: Bearer YOUR_JWT_TOKEN

Response:
{
    "totalCards": 15,
    "isMLActive": true,
    "recommendations": [
        {
            "flashcardId": 234,
            "optimalReviewHours": 2,
            "recommendedReviewDate": "2026-01-08T12:30:00Z",
            "confidence": 0.85,
            "reason": "Начальная стадия изучения, частые повторения",
            "priority": "high"
        }
    ]
}
```

### Пример: Персональные советы
```http
GET /api/ai/content/tips
Authorization: Bearer YOUR_JWT_TOKEN

Response:
{
    "total": 5,
    "tips": [
        "💡 Ваши результаты ниже среднего. Рекомендуем уделить больше времени базовым темам",
        "⚠️ Требуется улучшение по предметам: Физика, Химия",
        "🎯 Вы только начали обучение. Установите ежедневную цель - 10-15 карточек",
        "🔄 Вы быстро забываете материал. Попробуйте увеличить частоту повторений",
        "📝 Пройдите больше тестов для более точной оценки ваших знаний"
    ],
    "generatedAt": "2026-01-08T10:00:00Z"
}
```

## Заключение

UniStart интегрирует множество AI технологий для создания интеллектуальной образовательной платформы:

1. **ML.NET** - для адаптивного обучения
2. **Рекомендательные системы** - для подбора университетов
3. **Контентная персонализация** - для оптимизации обучения
4. **Генеративный AI** - для создания контента (в разработке)

Все компоненты работают вместе, анализируя поведение пользователя и адаптируя контент под его индивидуальные потребности.
