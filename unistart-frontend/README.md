# 🎓 UniStart Frontend

Современный фронтенд для образовательной платформы UniStart.

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
cd unistart-frontend
npm install
```

### 2. Запустите проект

```bash
npm run dev
```

Откроется на `http://localhost:3000`

### 3. Убедитесь, что Backend запущен

Backend должен быть доступен на `https://localhost:7000`

## 📦 Технологии

- **React 18** + **TypeScript**
- **Vite** - молниеносная сборка
- **Tailwind CSS** - утилитарный CSS
- **Framer Motion** - плавные анимации
- **React Router** - навигация
- **React Query** - кеширование API
- **Axios** - HTTP клиент
- **Recharts** - графики и аналитика
- **React Hook Form** - формы
- **Lucide Icons** - иконки

## 🎨 Дизайн

- **Цветовая схема:** Академический синий (#0EA5E9)
- **Шрифты:** Inter (основной), JetBrains Mono (моноширинный)
- **Responsive:** Mobile-first подход
- **Анимации:** Плавные переходы и микро-анимации

## 📁 Структура проекта

```
src/
├── components/
│   ├── common/       # Переиспользуемые компоненты
│   ├── layout/       # Layout (Header, Sidebar)
│   ├── flashcards/   # Компоненты карточек
│   └── quiz/         # Компоненты тестов
├── pages/            # Страницы
├── services/         # API сервисы
├── context/          # React Context
├── hooks/            # Custom hooks
└── types/            # TypeScript типы
```

## 🔧 Доступные команды

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm run preview  # Предпросмотр production сборки
npm run lint     # Проверка кода
```

## 🌐 API Integration

Backend API: `https://localhost:7000/api`

Все запросы автоматически проксируются через Vite dev server.

---

**Сделано с ❤️ для студентов Казахстана**
